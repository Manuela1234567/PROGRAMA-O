1 - Código de implementasção do algoritmo de ordenação Bubble Sort usando python
def bubble_sort(arr):
    n = len(arr)
    # Percorre todos os elementos do array
    for i in range(n):
        # Últimos i elementos já estão no lugar certo
        for j in range(0, n-i-1):
            # Percorre o array da posição 0 até n-i-1
            # Troca se o elemento encontrado for maior que o próximo elemento
            if arr[j] > arr[j+1]:
                arr[j], arr[j+1] = arr[j+1], arr[j]

# Teste da função
arr = [64, 34, 25, 12, 22, 11, 90]
bubble_sort(arr)
print("Array ordenado usando Bubble Sort:")
for i in range(len(arr)):
    print("%d" % arr[i], end=" ")


2- Código vogais
def vogais (str):
    for c in str:
        if c in "aeiou":
            print(c)
vogais ("arvore")
